// src/components/Loading.js
import React from 'react';
import './Loading.css'; // Optional: if you want to style the loading screen

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;
